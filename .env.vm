# Configuration Frontend - VM / Réseau local
# Utilisé pour le build déployé sur une VM (npm run build:vm).
# Remplacer <VM_IP> par l'IP de votre VM (ex: 192.168.1.50).

VITE_API_URL=http://192.168.1.109:8080/api
VITE_CSP_ALLOWED_HTTP_HOSTS=localhost,127.0.0.1,192.168.1.109
VITE_CSP_ALLOW_PRIVATE_NETWORK_HTTP=true
