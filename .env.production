# Configuration Frontend - Production
# Copier les valeurs depuis .env.example et adapter (domaine, HTTPS).

# URL du backend API en production (souvent même origine via nginx : /api)
VITE_API_URL=http://192.168.1.109:8080/api

# CSP : hôtes autorisés en HTTP (alignés avec le backend)
VITE_CSP_ALLOWED_HTTP_HOSTS=localhost,127.0.0.1,192.168.1.109
VITE_CSP_ALLOW_PRIVATE_NETWORK_HTTP=true
